{
  "name": "next-forge",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "bump-deps": "npx npm-check-updates --deep -u -x cmdk,eslint && pnpm install"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "eslint": "8",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "stylelint": "^16.6.1",
    "turbo": "latest",
    "typescript": "^5.4.5",
    "ultracite": "^3.8.0"
  },
  "engines": {
    "node": ">=18"
  }
}
