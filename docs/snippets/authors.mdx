export const Authors = ({ data }) => (
  <div className="-mt-8 mb-16 flex flex-col gap-2">
    <span className="text-gray-500 dark:text-gray-400 text-sm">Co-authored by</span>
    <div className="flex flex-wrap items-center gap-2">
      {data.map((author) => (
        <div
          key={author.name}
          className="card p-3 pr-4 inline-flex items-center gap-2 not-prose font-normal group relative ring-2 ring-transparent rounded-xl bg-white dark:bg-background-dark border border-gray-950/10 dark:border-white/10 overflow-hidden"
        >
          <div className="-space-x-1 flex items-center">
            <img
              className="m-0 overflow-hidden rounded-full object-cover"
              src={author.avatar}
              alt={author.name}
              width={24}
              height={24}
            />
            <img
              className="m-0 overflow-hidden rounded-full object-cover"
              src={`https://img.logo.dev/${new URL(author.logo).hostname}?token=pk_a7kMUiN0TCe8qXA6vbcfMw`}
              alt={author.name}
              width={24}
              height={24}
            />
          </div>
          <div className="flex flex-col">
            <span
              className="font-medium leading-tight"
              style={{
                fontSize: '13px',
              }}
            >
              {author.name}
            </span>
            <span
              className="text-gray-500 dark:text-gray-400 leading-tight"
              style={{
                fontSize: '11px',
              }}
            >
              {author.company}
            </span>
          </div>
        </div>
      ))}
    </div>
  </div>
);
